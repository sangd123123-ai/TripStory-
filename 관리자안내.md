# 관리자 안내

## 로그인 방법

- 관리자도 `/login` 페이지에서 로그인합니다.
- 로그인 성공 후 `role: admin`이면 `/admin` 대시보드로 이동합니다.
- `/admin/login`은 더 이상 기본 진입 경로로 쓰지 않습니다.

## 현재 배포 기준

- 프론트엔드: Vercel
  - `https://trip-story-rose.vercel.app`
- 백엔드 API: Render
  - `https://tripstory-api.onrender.com`

## 주요 환경변수

Render `tripstory-api` 서비스에 설정합니다.

```text
CLIENT_ORIGIN=https://trip-story-rose.vercel.app
KAKAO_REDIRECT_URI=https://tripstory-api.onrender.com/auth/kakao/callback
GOOGLE_REDIRECT_URI=https://tripstory-api.onrender.com/auth/google/callback
```

Vercel 프론트엔드 프로젝트에 설정합니다.

```text
REACT_APP_API_URL=https://tripstory-api.onrender.com
```

## OAuth 설정

- Kakao Developers redirect URI:
  - `https://tripstory-api.onrender.com/auth/kakao/callback`
- Google Cloud Console redirect URI:
  - `https://tripstory-api.onrender.com/auth/google/callback`

## 배포 후 확인

1. `https://tripstory-api.onrender.com/healthz`가 `{"ok":true}`를 반환하는지 확인합니다.
2. Vercel 사이트에서 일반 회원가입/로그인을 테스트합니다.
3. 카카오 로그인과 구글 로그인을 테스트합니다.

## 참고

- Render 무료 인스턴스는 비활성 상태 이후 첫 요청이 느릴 수 있습니다.
- `/uploads` 로컬 파일 저장은 호스팅 환경에서 영구 보장되지 않을 수 있으므로, 운영 안정성을 높이려면 Cloudinary/S3 같은 외부 스토리지 이전을 고려합니다.
